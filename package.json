{
  "name": "setupgateway",
  "version": "1.0.0",
  "description": "testing setting up an aws api gateway",
  "main": "setup.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.425.0"
  }
}
